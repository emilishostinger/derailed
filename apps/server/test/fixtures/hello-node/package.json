{
  "name": "hello-node",
  "version": "1.0.0",
  "private": true,
  "description": "A fixture with no Dockerfile, so the Nixpacks build path gets exercised.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=20"
  }
}
